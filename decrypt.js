// a = ['a','b',''];

var arr=[]; for (var idx='A'.charCodeAt(0),end='Z'.charCodeAt(0); idx <=end; ++idx){arr.push(String.fromCharCode(idx));} 

console.log(arr);


decrypt = (str)=>{
    let passobj = {};
    for(i=0;i<str.length;i++){
        
    }
}

str="12258"

